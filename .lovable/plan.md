### الهدف
إخفاء اختصارات "المحفظة" و"اشتراكي" و"طلباتي" من بطاقة الطالب في الصفحة الرئيسية، مع إبقائها ضمن صفحة الإعدادات فقط.

### التغييرات
1. **src/components/student/StudentProfileCard.tsx**
   - حذف ثلاثة عناصر Shortcut من شبكة الاختصارات:
     - `/subscription` (اشتراكي)
     - `/payments` (طلباتي)
     - `/wallet` (المحفظة)
   - تبقى فقط:
     - `/exams/history` (سجل الاختبارات)
     - `/settings` (الإعدادات)
   - تحديث نوع `Shortcut` ليعكس الروابط المتبقية.
   - إزالة الواردات غير المستخدمة (`CreditCard`, `Receipt`, `Wallet`) إن وجدت.

2. **src/routes/_authenticated/settings.tsx**
   - لا تعديل — أقسام "الاشتراك" و"المدفوعات والمحفظة" موجودة بالفعل وتخدم الغرض.

### ملاحظات
- لا تغيير على schema أو routes.
- الاشتراك summary يظل ظاهراً في البطاقة (SubscriptionBlock) لأنه إشعار حالة وليس اختصار.
- الاختصارات المخفية يمكن الوصول إليها من صفحة الإعدادات مباشرة.