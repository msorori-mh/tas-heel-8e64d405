# الخطة قوية جداً، لكن قبل أن أعتبر `14B` جاهزة للبناء أريد تصحيح **5 نقاط معمارية مهمة**؛ اثنتان منها مانعتان فعلاً لأنهما قد تسبّبان تعارض أكواد أو تجاوز عزل المسار.

```text
PAST_MINISTERIAL_EXAMS_FOUNDATION_14B
= GO_WITH_REQUIRED_CORRECTIONS

SHARED_DB_APPLY = NO
REAL DATA = NO

```

## 1. كود `mex` الحالي غير صالح — يجب أن يتضمن المسار والدور

الكود المقترح:

```text
mex-g12-001-2025-main

```

سيصطدم فوراً في حال:

```text
فيزياء مشتركة
+ صنعاء 2025 main
+ عدن 2025 main

```

لأن `subject_code` نفسه في TCS-2، بينما النموذج الوزاري Track-specific و`code` UNIQUE.

وكذلك لا يظهر `exam_round`.

أعتمد بدلاً منه:

```text
mex-{grade}-{subjectNo}-{trackCode}-{year}-{roundCode}-{variantCode}

```

مثال:

```text
mex-g12-001-sanaa-2025-r1-main
mex-g12-001-aden-2025-r1-main
mex-g12-001-sanaa-2025-r2-main
mex-g12-001-sanaa-2025-r1-b

```

وهذا **لا يخالف TCS-2**؛ المحتوى الدراسي Track-independent، لكن النموذج الوزاري نفسه Track-specific.

واجعل:

```text
exam_round_code
model_variant_code

```

Stable slugs وليسا نصوص عرض حرّة.

مثلاً:

```text
exam_round_code = r1
round_label = الدور الأول

model_variant_code = main
model_label = النموذج الرئيسي

```

---

## 2. يجب منع الانحراف بين عضوية الوزاري و`exam_template_questions`

هذه أهم نقطة بعد الكود.

لدينا الآن مجموعتان يمكن أن تصفا أسئلة الاختبار:

```text
exam_template_questions

```

و:

```text
ministerial_model_questions

```

إذا احتوى الأول على:

```text
Q1 Q2 Q3

```

والثاني على:

```text
Q1 Q2 Q4

```

سيؤدي الطالب اختباراً يختلف عن النموذج الوزاري الموثق.

لذلك عند Publish يجب أن يكون:

```text
SET(exam_template_questions.question_id)
=
SET(ministerial_model_questions.question_id)

```

بالضبط.

لا Missing ولا Extra.

وأضف اختبار:

```text
membership/template set mismatch = DENY PUBLISH

```

---

## 3. الأهم: يجب تثبيت Revision نفسها داخل Session

`ministerial_model_questions` تحفظ:

```text
question_revision_id

```

وهذا ممتاز.

لكن إذا كان `create_exam_session_with_snapshot` يأخذ فقط:

```text
question_id

```

ثم يستخدم أحدث Published Revision، فقد يحدث:

```text
النموذج الوزاري موثّق على Revision 3
↓
تم نشر Revision 4 لاحقاً
↓
جلسة الطالب تستخدم Revision 4

```

فتضيع أصالة السؤال الوزاري.

لذلك `create_ministerial_exam_session` يجب أن يضمن أن Snapshot يستخدم **بالضبط**:

```text
ministerial_model_questions.question_revision_id

```

وليس مجرد Current Published Revision.

هذه قاعدة:

```text
MINISTERIAL MODEL MEMBERSHIP
→ pins exact published revision
→ session snapshot pins same exact revision

```

وأريد اختبار خروج:

```text
model membership revision = R3
current published revision = R4

ministerial session must use R3

```

إذا كان محرك Snapshot الحالي لا يدعم ذلك، فهذا يجب أن يظهر كـ Blocker في 14B، وليس الالتفاف عليه.

---

## 4. يجب إغلاق تجاوز `create_exam_session_with_snapshot`

هذا خطر محتمل:

نبني:

```text
create_ministerial_exam_session(model_id)

```

مع فحص:

```text
student.track == model.track

```

لكن الطالب قد يستدعي مباشرة:

```text
create_exam_session_with_snapshot(ministry_template_id)

```

ويتجاوز `ministerial_model_id` بالكامل.

لذلك يجب أن تصبح القاعدة:

```text
if exam_templates.mode != ministry
→ generic creator allowed normally

if exam_templates.mode = ministry
→ generic creator DENY
→ must use create_ministerial_exam_session

```

أو تعديل المحرك بطريقة مكافئة fail-closed.

اختبار إلزامي:

```text
generic session creator + ministry template
= DENY_MINISTERIAL_SESSION_REQUIRES_MODEL

```

هذا مهم جداً.

---

## 5. لا أعطي الطالب SELECT مباشراً على `ministerial_model_questions`

أختلف هنا قليلاً مع الخطة الحالية.

حتى لو لم يوجد:

```text
correct_answer
explanation

```

في جدول العضوية، إعطاء الطالب القائمة كاملة يعني أنه يستطيع قبل بدء الاختبار معرفة:

```text
question_id
question_revision_id
display_order
original_question_number
section

```

وربما بناء Enumeration أو ربطه بمصادر أخرى.

الأكثر أماناً:

```text
ministerial_exam_models
Student SELECT published accessible models ✅

ministerial_model_questions
Student direct SELECT ❌
Content Staff/Admin ✅

```

والطالب يحصل على أسئلة الاختبار فقط عبر:

```text
create_ministerial_exam_session
→ safe session snapshot

```

أما بيانات المصدر التي نريد إظهارها بعد الاختبار، فتأتي عبر RPC/View محدود الصلاحيات وبعد تحقق حالة الجلسة.

هذا يتوافق أكثر مع قاعدة Answer-Leak التي شددناها سابقاً.

---

# تعديل صغير على الأمن

بدلاً من:

```sql
SET search_path = public

```

أعتمد النمط الأقوى الذي استخدمناه سابقاً:

```sql
SET search_path = public, pg_temp

```

ومع كل `SECURITY DEFINER`:

```text
REVOKE ALL ON FUNCTION ... FROM PUBLIC;
REVOKE ALL ON FUNCTION ... FROM anon;
GRANT EXECUTE فقط للأدوار المطلوبة.

```

---

# توجد أيضاً مفارقة في GRANTs الحالية

الخطة تقول:

```text
authenticated = SELECT only

```

وفي الوقت نفسه:

> طاقم المحتوى يدير الجداول عبر RLS.

إذا لم يكن لدى `authenticated`:

```text
INSERT / UPDATE / DELETE

```

فلن تصل العملية أصلاً إلى RLS.

أنا أفضل **عدم إعطاء Direct DML** هنا أساساً.

اعتمد:

```text
authenticated:
SELECT allowed resources only

DML:
protected RPC only

```

أي مستقبلاً إدارة:

```text
create ministerial model
update model
add/remove membership
publish
unpublish/archive

```

تمر عبر RPCs محمية بـ:

```text
is_content_staff
qb capability where relevant
full_admin for destructive operations

```

ولا Client PostgREST writes مباشرة.

---

# Publish Gate النهائي

أريد `publish_ministerial_model` يتحقق ذرّياً من كل التالي:

```text
active subject-track assignment              PASS
template.mode = ministry                     PASS
template active                              PASS
template belongs consistently to subject     PASS
membership count > 0                         PASS

membership questions == template questions   EXACT MATCH

every revision published                     PASS
every revision target matches subject        PASS

no duplicate display_order                   PASS
no duplicate revision membership             PASS

model code valid TCS-2 ministerial format     PASS
natural identity unique                      PASS

```

ثم:

```text
is_published = true
published_at = now()

```

واجعل هناك Check:

```text
is_published = true
⇒ published_at IS NOT NULL

```

---

# MODEL / STUDENT Gate

المفهومان يبقيان منفصلين:

```text
MODEL_VALIDITY_GATE

(model.subject_id, model.track_id)
must exist as active subject assignment

```

ثم:

```text
STUDENT_VISIBILITY_GATE

student.track_id = model.track_id
AND can_access_subject(model.subject_id)

```

وهذا يعطينا السلوك الصحيح:

```text
Shared Physics:
Sanaa ✅
Aden  ✅

Sanaa student:
Physics content           ✅
Sanaa ministry 2025       ✅
Aden ministry 2025        ❌

```

---

# حالات PG17 الإضافية

أضف إلى المصفوفة الحالية:

```text
mex code:
same subject/year/variant, different track          UNIQUE PASS
same subject/track/year, different round            UNIQUE PASS

generic creator + ministry template                 DENY

membership set != template question set             DENY PUBLISH

membership revision R3 / current revision R4
session snapshot                                    MUST PIN R3

student direct ministerial_model_questions SELECT   DENY

student safe session snapshot                       ALLOW

ordinary template generic session                   PASS UNAFFECTED

```

---

# الحكم

بعد دمج هذه التصحيحات، أمضوا مباشرة في كتابة Migration المعلّقة وتشغيل PG17:

```text
14B.1 AUDIT                         ✅
14B.2 MIGRATION DRAFT              ← الآن
14B.3 PG17 REHEARSAL
14B.4 SECURITY / ANSWER-LEAK
14B.5 REGRESSION
14B.6 REPORT

```

ولا Apply بعد.

المخرج الذي أريد رؤيته:

```text
MEX_CODE_TRACK_AND_ROUND_SAFE            PASS
MODEL_VALIDITY_GATE                      PASS
STUDENT_TRACK_ISOLATION                  PASS
GENERIC_MINISTRY_SESSION_BYPASS          CLOSED
EXACT_REVISION_PINNING                   PASS
TEMPLATE_MEMBERSHIP_PARITY               PASS
DIRECT_MEMBERSHIP_STUDENT_READ           DENY
ANSWER_LEAK                              ZERO
ANON_EXECUTE                             ZERO
ORDINARY_EXAMS_REGRESSION                PASS

PAST_MINISTERIAL_EXAMS_FOUNDATION_14B
= PASS_READY_FOR_APPLY

```

**بعد هذا التقرير فقط نقرر تطبيق 14B على القاعدة المشتركة.** هذه التعديلات الآن رخيصة جداً، لكنها تمنع لاحقاً أخطاء صعبة في أصالة النماذج الوزارية وعزل صنعاء/عدن.PAST_MINISTERIAL_EXAMS_FOUNDATION_14B — DB Foundation + PG17 Rehearsal

المرجع الوحيد: `docs/ministerial-exams/PAST-MINISTERIAL-EXAMS-ARCHITECTURE-14A.md` (TCS-2).
النطاق: Schema + RPC/Guards + بروفة PG17 معزولة فقط. **لا Apply على القاعدة المشتركة، لا UI، لا بيانات.**

## نتائج الفحص الفعلي للقاعدة (تمت قراءتها الآن)

- `exam_templates`: يحوي `mode exam_mode` (يشمل `ministry`)، `subject_id/unit_id/lesson_id` (كلها nullable, ON DELETE SET NULL)، `code` مع unique جزئي + trigger `assert_natural_code_immutable`. صالح لإعادة الاستخدام كما قررت 14A.
- `exam_template_questions`: `(template_id, question_id)` unique، بلا حقول مصدر (رقم السؤال الأصلي/الصفحة) — لذا نحتاج جدول عضوية وزاري منفصل كما في 14A.
- `exam_sessions`: لا يحوي أي عمود وزاري؛ `template_id` NOT NULL؛ سياسات: قراءة المالك + إدارة الأدمن.
- `subject_curriculum_tracks(subject_id, curriculum_track_id, is_active)` = PK مركب، مع فهرس جزئي على النشط → مصدر بوابة الصلاحية.
- `question_targets`: بلا `curriculum_track_id` (مطابق للمطلوب)، يحوي `revision_id NOT NULL` + FK مركب `(revision_id, question_id)` + trigger immutability. القراءة محصورة بطاقم المحتوى.
- توقيعات مؤكدة: `can_access_subject(_subject_id uuid)`، `create_exam_session_with_snapshot(p_template_id uuid)`، `has_active_subscription(_user_id uuid)`، `is_content_staff(_user_id uuid)`، `qb_has_capability(p_user_id, p_capability)`.
- بنية البروفة المعزولة موجودة: `tests/import/run-pg17-*-rehearsal.sh` + `tests/import/fixtures/pg17-baseline-schema.sql` — سنتبع نفس النمط.

## ما سيُبنى

### 1) ملف Migration معلّق (لا يُطبَّق)

`supabase/migrations-pending/20260814020000_ministerial_exams_foundation_14b.sql`

**الجداول**

- `ministerial_exam_models`: `id`, `exam_template_id UNIQUE → exam_templates(id) ON DELETE RESTRICT`, `subject_id`, `curriculum_track_id`, `academic_year int`, `exam_round text`, `model_variant_code text`, `model_label text NULL`, `code text UNIQUE` (TCS-2: `mex-g12-001-2025-main`), `is_published bool default false`, `published_at`, `created_by`, timestamps.
  - `UNIQUE(subject_id, curriculum_track_id, academic_year, exam_round, model_variant_code)`
  - بلا `grade_id` (يُستنتج من Subject).
  - trigger: `code` immutable + `updated_at`.
- `ministerial_model_questions` (جدول العضوية الوحيد): `model_id`, `question_id`, `question_revision_id`, `original_question_number`, `section_code`, `marks numeric`, `source_page`, `source_reference`, `display_order`، مع FK مركب `(question_revision_id, question_id)` نحو `question_revisions`، و`UNIQUE(model_id, question_revision_id)` و`UNIQUE(model_id, display_order)`.
  - لا أعمدة إجابة/حل/تفسير إطلاقاً.

**عمود ربط الجلسة**

- `exam_sessions.ministerial_model_id uuid NULL → ministerial_exam_models(id)` + trigger يتحقق أن `session.template_id = model.exam_template_id` و`session.mode` متسق.

**الدوال (SECURITY DEFINER, `set search_path = public`)**

- `assert_ministerial_model_track_valid()` — بوابة MODEL_VALIDITY_GATE: ارتباط نشط في `subject_curriculum_tracks`، وإلا `MODEL_TRACK_NOT_ASSIGNED_TO_SUBJECT`. تعمل كـ trigger على INSERT/UPDATE وتُستدعى ثانيةً عند النشر.
- `assert_ministerial_question_publishable()` — trigger على جدول العضوية: النسخة `published` + وجود `question_targets` صالح + `target.subject_id = model.subject_id`. لا اشتراط مسار على الهدف.
- `ministerial_model_can_publish(p_model_id)` + `publish_ministerial_model(p_model_id)` — fail-closed: ارتباط مسار نشط، template صالح (`mode='ministry'`, `is_active`)، عضوية غير فارغة، كل نسخة منشورة، كل هدف مطابق للمادة.
- `student_can_access_ministerial_model(p_model_id)` — عزل الطالب: `profiles.curriculum_track_id = model.curriculum_track_id` **و** `can_access_subject(model.subject_id)`. Sanaa→Aden = DENY حتى لو المادة مشتركة.
- تعديل مسيطر عليه لبدء الجلسة الوزارية: wrapper `create_ministerial_exam_session(p_model_id)` يستدعي المنطق الحالي ثم يضبط `ministerial_model_id`، دون تغيير سلوك `create_exam_session_with_snapshot` للأوضاع الأخرى.

**RLS + GRANT**

- تفعيل RLS على الجدولين الجديدين.
- طلاب: SELECT على النماذج **المنشورة فقط** ضمن مسارهم (بدون أي وصول لبيانات إجابة؛ العضوية تكشف metadata مصدر فقط، والسؤال نفسه يمر عبر snapshot الجلسة).
- طاقم المحتوى/الأدمن: إدارة حسب `is_content_staff` / `is_full_admin` فقط، بلا توسيع صلاحيات عام.
- `GRANT SELECT` للـ`authenticated` فقط + `GRANT ALL` لـ`service_role`؛ **لا anon** ولا `EXECUTE` لـ`anon` على أي دالة جديدة (سحب صريح من PUBLIC).

### 2) بروفة PG17 معزولة

`tests/import/run-pg17-ministerial-foundation-14b-rehearsal.sh` + fixtures جديدة، تغطي حرفياً مصفوفة الحالات المطلوبة (ALLOW/DENY) بما فيها: المادة المشتركة على المسارين، اختلاف `model_variant_code`، تكرار المفتاح الطبيعي، أهداف خاطئة، نسخة غير منشورة، عزل الطالب، منع تسريب الإجابة، منع anon، وعدم تأثر جلسات الاختبار العادية وبنك الأسئلة.

### 3) Regression

تشغيل: اختبارات QB، اختبارات الاستيراد/العقد، حراس TCS-2، `tsgo` typecheck، والبناء إن توفر.

### 4) التقرير

`docs/ministerial-exams/PAST-MINISTERIAL-EXAMS-FOUNDATION-14B-REPORT.md` بالحقول المطلوبة كاملة مع `SHARED_DB_APPLIED=NO` وقرار `PASS_READY_FOR_APPLY / NEEDS_REVISION`.

## خارج النطاق

لا Apply على القاعدة المشتركة، لا محتوى وزاري حقيقي أو وهمي، لا UI، لا نشر فعلي، ولا قالب استيراد وزاري (يؤجَّل إلى 14C).