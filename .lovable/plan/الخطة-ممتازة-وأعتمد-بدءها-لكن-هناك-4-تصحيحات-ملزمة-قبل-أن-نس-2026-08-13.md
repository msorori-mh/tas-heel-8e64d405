# الخطة ممتازة، وأعتمد بدءها، لكن هناك **4 تصحيحات ملزمة** قبل أن نسمح للحكم النهائي `PASS`:

```text
CONTENT_AND_QUESTION_UNIFIED_OPERATIONAL_E2E_09
= GO_WITH_REQUIRED_CORRECTIONS

SCHEMA_CHANGE = NO
PRODUCTION_CHANGE = NO
NON_PROD_ONLY = YES

```

### 1. لا تستخدم الترتيب الرقمي `01→09`

هذه أهم نقطة. يجب أن يعمل المشغّل وفق **Dependency Graph المعتمد فعلياً**، وليس أرقام القوالب.

سبق أن كان ترتيب التنفيذ المعتمد يتضمن تقديم Template 09 على القوالب التي تحتاج `question_code`. لذلك لا أعتمد العبارة:

```text
... تقييم ← ربط ← أسئلة

```

إذا كان الربط يعتمد على سؤال موجود.

المشغّل يجب أن يستورد:

```text
Dependency Graph from import-contract.ts
→ topological order

```

ويضيف اختباراً:

```text
runner order === contract dependency order

```

ولا يكرر ترتيباً hard-coded منفصلاً يمكن أن ينحرف لاحقاً.

---

### 2. فحص الطالب يصبح إلزامياً للحكم `PASS`

هذه العبارة غير مناسبة للبوابة النهائية:

> إذا لم يُضبط `E2E_STUDENT_USER_ID` تُسجل الحالات skipped.

يمكن السماح بذلك أثناء التطوير، لكن **ليس في تشغيل الإغلاق**.

القرار:

```text
E2E_STUDENT_USER_ID missing
→ UNIFIED_E2E_INCOMPLETE
→ NOT PASS

```

لأن:

```text
draft visibility = zero
answer leakage = zero

```

شرطان أمنيان أساسيان، وليس اختبارين اختياريين.

---

### 3. ثبّت حدود الذرّية الصحيحة

Pass 5 صحيح بشرط أن يوثق بوضوح:

```text
Atomicity = per-template
NOT whole-package

```

أي إذا نجحت القوالب A وB ثم فشل C:

```text
A/B remain committed
C = full rollback
later dependent templates = NOT EXECUTED

```

ولا نحاول rollback للحزمة كلها لأن هذا **ليس العقد الحالي**.

أضف تحققاً أن القوالب اللاحقة التي تعتمد على القالب الفاشل:

```text
were never started

```

وليس فقط أنها لم تفسد.

---

### 4. لا تجعل Teardown يمحو سجل التدقيق

لا أريد أن يعني:

```text
no e2e-* rows remaining

```

حذف:

```text
import_jobs

```

أو أي Audit history مطلوب تشغيلياً.

الصحيح:

```text
domain e2e entities        → deleted
QB test entities           → deleted safely
review states              → cleaned with entities
staging payload if policy allows → purged/expired
import_jobs                → retained for audit

```

وبالنسبة للفشل لا نشترط بالضرورة:

```text
zero staging rows

```

إذا كان تصميم النظام يحتفظ بها للتدقيق. نشترط:

```text
zero dangling applying state
zero unintended domain writes
job has terminal failed/blocked state
staging rows cannot be executed accidentally later

```

---

## بوابة الخروج المعتمدة

أعتمد بقية الخطة كما هي، وتصبح النتيجة النهائية:

```text
dependency order from contract          PASS

first unified import                     PASS
full exact replay                        PASS
partial content update                   PASS
question new target                      PASS
per-template failure rollback            PASS
dependent templates stop after failure   PASS

content review/hash reset                PASS

real student draft visibility            ZERO
real student draft question access       ZERO
real student answer leakage               ZERO

question auto-publish                     ZERO
current_published_revision_id             NULL for new imported questions

domain teardown                           PASS
audit jobs retained                       PASS

07 regression                             32/32 PASS
08 regression                             18/18 PASS
import contract                           60/60 PASS
question-bank-import                      ALL PASS

ZERO CRITICAL
ZERO HIGH

```

وعندها نعتمد:

```text
CONTENT_AND_QUESTION_UNIFIED_OPERATIONAL_E2E_09 = PASS

CONTENT_IMPORT_01_08 = READY
QUESTION_IMPORT_09 = READY
UNIFIED_IMPORT_PIPELINE = READY

CONTENT_UPLOAD_PIPELINE_NON_PROD = COMPLETE

```

وبعد هذا **لا نحتاج تطويراً جديداً لمسار الاستيراد** إلا إذا كشف الاختبار خللاً حقيقياً. الخطوة التالية تصبح مباشرة **Production Content Import Readiness Review** ثم تجهيز أول دفعة محتوى حقيقية للرفع.CONTENT_AND_QUESTION_UNIFIED_OPERATIONAL_E2E_09

اختبار حزمة الاستيراد كاملة (01→09) كوحدة واحدة على مادة/درس تجريبي، وفق Dependency Graph الحقيقي، دون بناء أي بنية جديدة.

## ما سيُنفَّذ

مشغل واحد جديد `tests/e2e/content-import/run-unified-import-e2e-09.ts` يشغّل الحزمة بالترتيب التبعي الصحيح في جلسة موظف حقيقية (JWT + RLS)، ثم يتحقق من الحالة النهائية للمحتوى والأسئلة ومن رؤية الطالب.

مراحل التشغيل داخل المشغّل:

```text
Pass 1  رفع الحزمة 01→09 كاملة لأول مرة
Pass 2  إعادة نفس الحزمة بالكامل            ⇒ Idempotency
Pass 3  تعديل جزئي (درس + سؤال)             ⇒ Update / New Draft Revision فقط
Pass 4  سؤال موجود + Target جديد            ⇒ TARGET_ADDED بلا نسخة جديدة
Pass 5  حقن ملف فاسد داخل الحزمة            ⇒ فشل معزول، بقية المنظومة سليمة
Pass 6  فحوصات الطالب + حالة المراجعة
Pass 7  Teardown كامل لبيانات e2e-*
```

## حالات التحقق (Exit Gate)


| #   | التحقق                                                                                                |
| --- | ----------------------------------------------------------------------------------------------------- |
| 1   | ترتيب التبعية يُحترم: مادة ← وحدة ← درس ← محتوى/شرح/موارد ← تقييم ← ربط ← أسئلة                       |
| 2   | الحزمة الأولى: كل قالب inserted بالعدد المتوقع، بلا أخطاء                                             |
| 3   | الإعادة: 0 inserted / 0 updated لكل القوالب (Idempotency على مستوى الحزمة لا الملف)                   |
| 4   | التعديل الجزئي: الملفات غير المعدّلة تبقى skipped، والمعدّل فقط updated                               |
| 5   | سؤال موجود بوجهة جديدة: نسخة المحتوى نفسها + صف وجهة إضافي                                            |
| 6   | ملف فاسد داخل الحزمة: تراجع ذرّي لذلك القالب فقط، والقوالب السابقة تبقى صحيحة، ولا صفوف staging عالقة |
| 7   | حالة المراجعة (content_review_state) مضبوطة للكيانات الجديدة وتُعاد ضبطها عند تغيّر البصمة            |
| 8   | الطالب لا يرى الدرس/المادة غير المنشورة عبر RPCs المحتوى                                              |
| 9   | الطالب لا يرى الأسئلة المسودة، ولا الخيارات، ولا الإجابة الصحيحة (تسريب = صفر)                        |
| 10  | لا نشر تلقائي: `current_published_revision_id` يبقى فارغاً لكل أسئلة الحزمة                           |
| 11  | التنظيف: لا صف `e2e-*` متبقٍّ، وعدّادات النطاقات تعود لخط الأساس                                      |


## عدم الانحدار (Regression)

بعد نجاح المشغّل الموحّد تُعاد الحزم الحالية كما هي دون تعديل:

- `run-content-import-e2e-07.ts` (32)
- `run-question-import-e2e-08.ts` (18)
- `npm run test:import-contract` (60)
- `npm run test:question-bank-import`

النتيجة النهائية تُعتبر PASS فقط إذا نجحت الحزمة الموحّدة والحزم الثلاث السابقة معاً.

## التفاصيل التقنية

- **Fixtures**: توسيع `scripts/content-import/generate-e2e-fixtures.mjs` بحزمة موحّدة بادئتها `u09-` (مادة/وحدة/درسان/محتوى/شرح/موردان/تقييم/ربط/ثلاثة أسئلة) + نسخة معدّلة جزئياً + نسخة وجهة جديدة + ملف فاسد واحد. البادئة تبقى ضمن `e2e-` ليعمل الـ teardown القائم.
- **مسار التنفيذ**: يُعاد استخدام `runCycle` (validate → `createContentImportExecutionJob` → `stageContentImportRows` → `executeContentImport`) بلا أي تعديل على كود الإنتاج؛ المشغّل الموحّد يستدعي نفس الدوال المستخدمة في لوحة الإدارة.
- **الطالب**: جلسة طالب حقيقية عبر `E2E_STUDENT_USER_ID` (اختياري؛ إذا لم يُضبط تُسجَّل الحالات كـ skipped صراحةً بدل PASS صامت).
- **التنظيف**: `purgeE2eQuestions` القائم + حذف صفوف النطاقات ببادئة `e2e-` فقط، مع التقاط خط أساس قبل التشغيل ومقارنته بعده.
- **حواجز الأمان**: يرفض التشغيل بدون `RUN_CONTENT_IMPORT_E2E=1`، ولا يكتب أي صف خارج بادئة `e2e-`، ولا يلمس الإنتاج.
- **توثيق**: تقرير إغلاق `docs/unified-import-e2e-09.md` بجدول النتائج وأمر التشغيل.

## خارج النطاق

لا تغييرات على schema، ولا على قواعد RLS، ولا على مسار الاستيراد نفسه — إلا إذا كشف الاختبار خللاً حقيقياً، وعندها يُصلَح الخلل ويُعاد تشغيل الحزم كاملة.