name: Temporary safe dependency lock refresh

on:
  push:
    branches:
      - codex/release-gate-ci-20260826

permissions:
  contents: write

jobs:
  refresh-lock:
    if: "!contains(github.event.head_commit.message, '[skip-lock-refresh]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/release-gate-ci-20260826
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Refresh lock with non-breaking audit fixes only
        run: npm audit fix --package-lock-only --ignore-scripts
      - name: Commit generated lock when changed
        run: |
          if git diff --quiet -- package-lock.json; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "security: refresh dependency lock without force [skip-lock-refresh]"
          git push origin HEAD:codex/release-gate-ci-20260826
