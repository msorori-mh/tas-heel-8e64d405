# TAMKEEN_QURAN_REAL_CONTENT_RECOVERY_18A1 — تقرير الإغلاق

**الحكم النهائي: PASS**

## 1. النطاق
استعادة محتوى دروس مادة **القرآن الكريم – الصف الأول الثانوي** (`subject_id = 1234e882-b0b2-499a-bd66-f91f480e1081`) حصراً.

- **بدون Migration** — لم تُنشأ أو تُعدَّل أي كائنات قاعدة بيانات.
- **بدون إعادة تشغيل قالب 03 (الدروس)** — معرفات الدروس الحالية بقيت كما هي.
- لم تُعدَّل أي شيفرة تطبيق ضمن هذه المهمة.

## 2. السبب الجذري (مؤكَّد من 18A)
إعادة تنفيذ قالب **03 — الدروس** أعادت إنشاء صفوف `lessons`، و`ON DELETE CASCADE`
حذف المحتوى التابع في `lesson_book_contents` و`lesson_resources`، ولم يُعَد تشغيل
قالبَي **04** و**06** بعدها. النتيجة: 21 درساً موجوداً بلا محتوى
(«محتوى الدرس لم يُضف بعد»).

## 3. مصدر بيانات الاسترجاع
أُعيد بناء ملفات Excel **حرفياً** من صفوف `import_staging_rows` الأصلية،
دون أي تحرير يدوي للمحتوى:

| القالب | Job المصدر (staging) | عدد الصفوف |
|---|---|---|
| 04 — محتوى الكتاب | `90ee8850-7f90-414a-a592-347f4b9f08ac` | 21 |
| 06 — الموارد | `a80e6712-7200-42a6-a10f-c4f08522853c` | 21 |

## 4. المرحلة 1 — Pilot (درس واحد)
الدرس: `lesson-g10-001-001` (`6fff752f-c6e5-46b1-8789-5cb11ad8d4c7`) —
«سورة السجدة: مراجعة الآيات الكريمة والدلالات».

- قالب 04: فحص ← تجهيز ← تنفيذ → **1 inserted**.
- قالب 06: فحص ← تجهيز ← تنفيذ → **1 inserted**.

### التحقق بحساب طالب حقيقي
الطالبة: `2345bfb2-9524-424b-b17b-b70d61689e50` (الصف الأول الثانوي – عدن)،
جلسة معاينة حقيقية عبر المتصفح بمقاس موبايل (420px، RTL).

النتيجة المرصودة:
- صفحة المادة: 21 درساً، شريط تقدم `0/21`.
- صفحة الدرس: بطاقة **«خطوة 1 — اقرأ الدرس»** تعرض محتوى الكتاب،
  وبطاقة **«خطوة 2 — موارد إضافية»** تفتح المورد بالرابط الفعلي
  `https://drive.google.com/file/d/1cb_MkmG-…/view` بعنوان «ملف PDF للطباعة».
- اختفت رسالة «محتوى الدرس لم يُضف بعد».
- **0 أخطاء Console**، ولا أي خطأ RLS أو صلاحيات.

الحكم: **PILOT = PASS** → الإذن بالانتقال إلى Bulk.

## 5. المرحلة 2 — Bulk Recovery
نُفّذت عبر مركز الاستيراد بنفس المسار الإلزامي (فحص → تجهيز → تنفيذ)
باستخدام ملفَي 21 صفاً لكل قالب.

| المؤشر | قبل | بعد |
|---|---|---|
| `lesson_book_contents` (المادة) | 1 (الـ Pilot فقط) | **21** |
| `lesson_resources` (المادة) | 1 (الـ Pilot فقط) | **21** |
| دروس بلا محتوى كتاب | 20 | **0** |

التنفيذ كان Idempotent: إعادة تمرير صف الـ Pilot لم تُنتج تكراراً.

## 6. التحقق بعد Bulk
دروس إضافية فُتحت بحساب الطالبة نفسها:
- `bdbdcab8-…` «القرآن الكريم وحفظه وتلاوته وفضله وآدابه» → محتوى + موارد ✅
- `92380b62-…` «موقف المؤمنين من المستهزئين بآيات الله» → محتوى + موارد ✅

لا وجود لأي درس يعرض «محتوى الدرس لم يُضف بعد» في هذه المادة.

## 7. ملاحظات تشغيلية
- `delivery_mode` بقي `in_app_content`؛ ملف الـ PDF يُقدَّم عبر رابط Drive
  داخل محتوى الكتاب وبطاقة الموارد. لا يوجد `is_primary` في بيانات الـ staging
  الأصلية، لذا لم يُرفَع أي مورد كمورد أساسي — وهذا مطابق للملف الأصلي.
- **توصية وقائية:** أي إعادة تنفيذ لقالب 03 يجب أن يتبعها إلزامياً
  إعادة تنفيذ 04 → 05 → 06 لنفس المادة، وإلا يتكرر نفس العطل.

## 8. الحكم
**TAMKEEN_QURAN_REAL_CONTENT_RECOVERY_18A1 = PASS**
