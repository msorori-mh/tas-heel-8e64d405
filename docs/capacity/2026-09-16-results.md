# Capacity execution results — 16 September 2026

**Decision: HOLD at 500; 10,000-user readiness is not established.**

Evidence: [external run 35048327715](https://github.com/msorori-mh/tas-heel-8e64d405/actions/runs/35048327715),
source `3cbda784650c8498950b0c74d5ffc27d4df251f5`, staging only.
Raw aggregate reports are committed in `evidence/2026-09-16/`.

| Concurrent authenticated test users | Requests | Request failures | Overall p95 | Slowest endpoint p95 | Completed journeys | Result |
| ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 5 | 889 | 0 | 358 ms | 480 ms | 52 | PASS |
| 25 | 4,343 | 0 | 342 ms | 657 ms | 254 | PASS |
| 100 | 12,238 | 0 | 1,221 ms | 1,941 ms | 714 | PASS |
| 500 | 4,206 | 309 (7.35%) | 15,001 ms | 15,001 ms | 191 | HOLD |

Stages requested 90 seconds. Passing stages took 98–100 seconds including completion
of work already started. The 500 stage stopped early at the >5% emergency error gate
and drained at 62.25 seconds. Failures were client request timeouts at 15 seconds,
concentrated in profile/catalog/performance reads. These are failures, not retried
away or excluded from the denominator. No 1,000+ stage was run after this failure.

At 100, measured throughput was 122.68 HTTP requests/s and p99 was 2,127 ms.
At 500, successful traffic slowed while 309 journeys failed; throughput was only
67.57 requests/s including failures. Generator event-loop p99 stayed about 21 ms
and RSS was under 200 MB; this rules out observed event-loop saturation, not every
possible network or generator limit.

## What this proves and excludes

Independent authenticated TEST_ONLY sessions exercised scoped profile/catalog reads,
lesson visibility, durable progress writes, idempotent replay, cross-account progress
denial, and ministerial creation, one text answer, submission and readback. All 100
accounts had submitted exam records in the database after the passing 100 stage.
The final runner additionally requires a completed journey for every VU and checks
denial against another pre-existing profile (not just potentially absent progress).
A five-user functional check of those additions completed for every account with
112 successful requests, including five profile denials, and no journey errors.
Its local network latency gate was HOLD; it is not external performance evidence.

This is a repeated full-journey stress profile with randomized two-second think time,
one Grade 12 subject and one 13-question model, answering one question per attempt.
It is not a measured mix of real student behavior. The highest passing sampled
stage is 100, not a universal maximum student capacity. No one-hour soak, distributed
10k run, refresh storm, Google OAuth, APK/browser rendering or content-byte/CDN test
has passed under this program.

## Diagnosis and next gate

During sampling the database had 23 connections against a configured maximum of 60,
with no observed lock waits. This snapshot does not prove absence of short spikes.
Profile SELECTs in cumulative PostgreSQL statistics averaged 0.09–0.17 ms, while
their HTTP requests timed out under load. Queueing, gateway/pool behavior and shared
resource contention therefore need direct metrics before attributing the problem
to the profile SQL or increasing connection limits.

The performance RPC's cumulative mean rose to about 106 ms and its recorded maximum
was 1,887 ms. A separate cold diagnostic initially took 2,050 ms versus 43 ms with
JIT disabled, but repeated on/off comparisons were 26–53 ms with identical results.
That did not establish a repeatable JIT cause. No global or function JIT setting was
changed, and no speculative index or query rewrite was deployed.

CAP-02 remains HOLD at 500. Next: obtain CPU, memory, I/O, gateway/pool wait and Auth
metrics around a controlled repeat, identify the limiting resource, apply an isolated
fix or measured compute change, then rerun the same gate. Raising timeout or relaxing
latency/error thresholds is not remediation.

CAP-03 remains HOLD for resource/data parity. The Supabase connector exposes staging
only and denied a production read, but the later Lovable SQL probe succeeded (see
addendum below). Hosting quotas and provider CPU/memory/I/O telemetry remain unavailable.
User authorization is already present. No paid upgrade was made. Distributed/session-
refresh/soak/CDN/alert deployment work is not marked complete.

## Delivered application change and cleanup

The application change spreads offline retry/reconnect traffic and coalesces duplicate
signals, retaining durable mutation IDs. This reduces synchronized reconnection pressure;
it is not claimed to resolve the 500-user API failure. It requires no production migration
and can be reverted independently. Fifteen targeted offline tests and nine load-runner
contract tests passed; CI verifies build and maintained suites separately.

After the external job stopped, cleanup used a frozen 500-ID/email manifest with matching
TEST_ONLY/run metadata. It revoked test sessions and deleted test users with normal FK
cascades. Transaction checks verified no test profiles, progress, sessions, exams or
offline mutation rows remained, and all 33 retained Auth-user fingerprints were unchanged.
The retained-user fingerprint check covers the cleanup transaction, not a historical
full-database fingerprint from before test setup. Final run-key mutation count: zero.

## Production follow-up after the initial load result

Lovable SQL access succeeded at 03:12 UTC after an initial cancelled request. Production
PostgreSQL is 17.6 on ARM, with max_connections=60, 22 observed connections and about
1.45 GB of database data. Staging was x86 with about 222 MB at inventory time. Matching
connection limits do not establish matching compute or representative data volume.
Critical function fingerprints matched; four UID policy expressions and several
indexes differed. Staging's existing answer index was composite `(session_id,
exam_session_question_id)`, not a direct leading-column lookup by question identifier.

The separate migration `20260916031703_capacity_answer_lookup_and_uid_initplans.sql`
adds a single-column answer lookup index to both environments and promotes the four
existing staging UID initplans to production, preserving owners/roles/admin rules.
Production application used an explicit transaction, 2-second lock timeout and
30-second statement timeout through the authorized Lovable SQL interface. The staging
migration is recorded by Supabase; production execution is documented here rather
than inventing a Supabase migration-history entry. Replay is idempotent.

Validation: a real staging transaction tested owner read/insert/update, other-user
read/insert/update denial and administrator reads under the authenticated role. All
passed and the fixture rolled back. Production policy fingerprints then matched
staging. Its answer lookup changed from a sequential scan filtering 332 rows to an
index scan using the new valid/ready index. Observed execution times were 11.457 ms
before and 0.112 ms after, but cache state differed: these are diagnostics, not a
controlled speedup claim. No production student rows were inserted or modified.

The original 500-user HOLD predates this follow-up. A fresh controlled load run is
required to measure its effect; this change does not establish 500 or 10,000 capacity.

Client deployment: PR274 merged as `706ff657fb42b653e383899170521e260e241d00`;
Lovable deployment `fd702345-5b72-4b6b-95ad-ad2d14de766c` was requested against that SHA.
Live browser verification found `offline-sync-fC3smVLF.js` containing the bounded
retry jitter and `500 + floor(random * 4500)` reconnect spread; the student page rendered.
