# STUDENT-APP-PRODUCTION-READINESS-CHECKLIST-01
## قائمة تحقق الجاهزية للإنتاج وتأكيد الإطلاق النهائي (Corrected 02)

- **التاريخ:** 2026-08-02
- **المشروع:** تطبيق طلاب الثانوية العامة (`msorori-mh/tas-heel-8e64d405`)
- **الـ HEAD المراجع:** `93127008143fc9ab1e37096c47a60cf93809dcda`
- **حالة العلاقات:** PR #48 `MERGED` في الفرع الرئيسي
- **نوع الوثيقة:** قائمة تحقق محكومة ببوابات الإطلاق السبع ومصنفة حسب الحالات التشغيلية

---

## 0. التمييز التشغيلي المستقل بين الحالات (Operational States Framework)

تُقيم جميع بنود قائمة التحقق بناءً على الإطار التشغيلي الموحد:
- **`source merged`**: الكود دُمج في main (مثل PR #48).
- **`source reviewed`**: كود المهاجرة والبرمجيات مراجع مصدرياً ومستندياً.
- **`local compilation`**: تجميع واختبار الكود والمهاجرة محلياً (Docker / Vitest).
- **`remote migration applied`**: تنفيذ المهاجرة فعلياً على قاعدة بيانات بعيدة (تتطلب موافقة المالك).
- **`production verified`**: التحقق في بيئة الإنتاج الحية بأدلة remote حصرية.

---

## 1. الأمان والحماية (Security & RLS Readiness)

- [x] **تأمين جدول الوحدات (`units`):** تقييد الاستعلام للمستخدمين المسجلين فقط (`SOURCE REVIEWED & LOCAL TESTED`: `20260731180000` - يتطلب دليلاً بعيداً مستقلاً للإنتاج).
- [x] **تأمين إجابات الاختبارات عمودياً:** سحب عمودي لأعمدة `correct_index` لتعذّر الاستعلام عبر PostgREST (`SOURCE REVIEWED & LOCAL TESTED`: `20260731120000`).
- [x] **حظر الدالة التفقدية للأدوار:** إغلاق ثغرة GUC وحظر التجاوز الذاتي لدوال SECURITY DEFINER في بنك الأسئلة (`QB-01 SOURCE MERGED & LOCAL TESTED`).
- [ ] **تشديد سلات التخزين (Storage Buckets):** إضافة قيود نوع الملف (MIME types) وحجم الملف لإيصالات شحن المحفظة (PENDING).
- [x] **حماية بوابات الوصول المجاني والمدفوع:** تفعيل دوال `student_can_access_unit` و `student_can_access_lesson` (`SOURCE REVIEWED`).

---

## 2. بنك الأسئلة والمحرك الأكاديمي (Question Bank & Attempt Engine)

- [x] **اعتماد المخطط الهيكلي (QB-01):** 15 جدولاً، دوال النشر والتوجيه، التوقيع الرقمي `payload_hash` (`SOURCE MERGED` عبر PR #48, `LOCAL COMPILATION PASS` 37/37).
- [ ] **تطبيق المهاجرة الهيكلية على السيرفر البعيد (Remote DB Apply):** تنتظر موافقة المالك الصريحة وبوابة المهاجرة البعيدة (PENDING REMOTE APPLY).
- [ ] **تأسيس ترحيل الأسئلة القديمة (QB-02 Backfill Prep):** الحزمة التالية الموصى بها لإعداد التوقيعات وتحويل البيانات محلياً (PENDING LOCAL PREP).
- [ ] **تحويل نمط التشغيل (QB-03 Cutover):** تحويل `attempt_pin_mode` من `LEGACY` إلى `QUESTION_BANK` (PENDING REMOTE CUTOVER).
- [ ] **واجهة التحرير متعددة النسخ (PKG-04):** تحديث لوحة التحكم لدعم النسخ والتصحيح المقالي (IN WAVE A PARALLEL).

---

## 3. المحتوى التعليمي والتنظيف (Content Readiness & Data Hygiene)

- [x] **ملاءمة قوالب الاستيراد 01-09:** توفر القوالب الرسمية وموافقتها لتقسيم مواد الصف الأول الثانوي (VERIFIED).
- [x] **فاحص ما قبل الاستيراد المحلي:** توفر `validate-content-package.mjs` ودليل التشغيل (VERIFIED).
- [ ] **تنظيف بيانات QA التجريبية (PKG-01):** حزمة إنتاجية مؤجلة (`Deferred Production Package`) تتطلب موافقة صريحة **ولا تُنفذ أثناء QB-02** (DEFERRED PENDING APPROVAL).
- [ ] **الاستيراد الفعلي للمنهج (PKG-11):** تنفيذ استيراد المواد والدروس والأسئلة الرسمية على البيئة البعيدة (PENDING WAVE B).

---

## 4. تجربة الطالب والـ PWA و Android (Student UX & Mobile Readiness)

- [x] **دعم اللغة العربية والوضع الداكن:** تصميم متجاوب كامل مع RTL وتصميم بصري أنيق (PASS).
- [x] **التسليم التلقائي للاختبارات الصارمة:** إنهاء وتسليم الجلسات المنتهية وقتياً تلقائياً (PASS).
- [x] **استبعاد `/exams` من التخزين المؤقت:** ضمان عدم تعليق جلسات الاختبارات الشديدة عند ضعف الشبكة (PASS).
- [ ] **دعم التخزين المحلي والعمل بدون إنترنت (PKG-07):** حفظ التمارين في IndexedDB ومزامنتها خلفياً (WAVE A).
- [ ] **ملف الربط الرقمي لأندرويد (PKG-10):** توفر `assetlinks.json` في `.well-known/` لتفعيل TWA (WAVE A).
- [ ] **ربط الإشعارات الفورية (PKG-08):** استقبال التنبيهات الفورية عبر FCM / WebPush (WAVE A).

---

## 5. الاختبارات الآلية وخط الإنتاج (Automated Testing & CI/CD)

- [x] **اختبارات الوحدة لخدمات التطبيق:** 25/25 PASS (`npm test`).
- [x] **اختبارات أمان وبنية بنك الأسئلة:** 37/37 PASS (Local Disposable Docker / Vitest).
- [x] **اختبار المتجهات الذهبية للتشفير:** 12/12 PASS (`npm run test:question-bank-hash`).
- [x] **اجتياز فحص حزم CI على Node 22:** إصلاح قفل الحزم `npm ci` وتمرير `tsc` والبناء (`web-ci.yml`).
- [ ] **إضافة فحوصات بنك الأسئلة و الـ E2E في CI (PKG-12):** تضمين جميع الاختبارات الشاملة في GitHub Actions (WAVE A).

---

## 6. موقف المسار الحرجة والبوابات (Critical Path & Release Gates Status)

### أ. المسار الحرجة (Critical Path):
1. **[DONE] PR #48 Merged:** دمج بنك الأسئلة الأساسي مصدرياً في main.
2. **[NEXT] QB-02 Import Foundation:** بناء وتأسيس محول الأسئلة القديمة محلياً.
3. **[PENDING] Oracle Review:** مراجعة ميكانيكية قاعدة البيانات والأداء.
4. **[PENDING APPROVAL] Remote Apply & Cutover:** التطبيق والترحيل البعيد بموافقة المالك.

### ب. حالة بوابات الإطلاق السبع (7 Release Gates):
1. **Source Gate:** `PASSED` (الكود مدمج في main والمحليات ناجحة).
2. **Local DB Gate:** `PASSED` (اجتياز تجميع واختبار Docker محلياً).
3. **Remote Migration Gate:** `PENDING` (تتطلب موافقة المالك لتطبيق QB-01 بعيداً).
4. **Content Gate:** `READY FOR DRY-RUN` (الفاحص والقوالب جاهزة).
5. **E2E Gate:** `IN WAVE A` (تنفذ ضمن PKG-12).
6. **Android Gate:** `IN WAVE A` (تنفذ ضمن PKG-10).
7. **Production Launch Gate:** `HOLD` (لحين اجتياز البوابات 3 إلى 6).

---

## 7. القرار النهائي والتفويضات (Release Approval Sign-off)

| المجال | الحالة التشغيلية | الملاحظات / المتطلبات |
|---|---|---|
| **الاستقرار الفني** | `local compilation PASS` | الكود محكم، 0 خطأ تجميع، جميع اختبارات الوحدة تنجح. |
| **الأمان و RLS** | `source reviewed PASS` | تم سد ثغرات التسريب مصدرياً، وتستلزم دليلاً بعيداً للإنتاج. |
| **بنك الأسئلة (QB-01)** | `source merged` (PR #48) | المصدر مدمج ومختبر محلياً 100%، ينتظر قرار المالك بالتطبيق البعيد. |
| **الحزمة التالية** | `QB-02 Import Foundation` | البَدء الفوري بتأسيس ترحيل الأسئلة محلياً. |
| **تنظيف QA** | `Deferred Production` | حزمة مؤجلة تتطلب موافقة صريحة ويُحظر تشغيلها أثناء QB-02. |
| **القرار الكلي** | `PASS_WITH_NOTES` | **جاهزية كاملة للبدء بأعمال الموجة A بالتوازي وتأسيس QB-02 محلياً.** |
