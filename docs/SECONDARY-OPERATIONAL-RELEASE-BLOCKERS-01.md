# SECONDARY-OPERATIONAL-RELEASE-BLOCKERS-01 — سجل العوائق

المرجع: main @ `10cb220` + PRs #27/#28/#29 المفتوحة. التاريخ: 2026-07-31.

## P0 — تمنع الإطلاق (يجب أن تُغلق أو تُقبل كتابةً من المالك)

| # | العائق | الشدة | المصدر | الحالة | المسار |
|---|---|---|---|---|---|
| P0-1 | **تسريب إجابات الاختبارات عبر PostgREST:** سياسة SELECT على `questions` تعيد `correct_index`/`explanation` لأي طالب له وصول للمادة، و`exam_template_questions` مقروءة لأي نموذج نشط — التفاف كامل على تعتيم RPC والعميل | HIGH | WAVE-2 | **مفتوح — NEEDS_OWNER_APPROVAL** | migration (فصل أعمدة الإجابة / عرض security barrier / RPCs فقط) — **لا تُنفَّذ دون موافقة** |
| P0-2 | **جاهزية بيانات المحتوى غير مؤكدة:** لا يمكن قياس عدد المواد/الوحدات/الدروس/الأسئلة/نماذج الاختبار الجاهزة | مانع قرار | WAVE-3 | مفتوح | جرد read-only بواسطة المالك أو حساب admin-test |
| P0-3 | **لا حسابات اختبار:** E2E الحية لكل الأدوار متعذرة | مانع تحقق | WAVE-1/2/4 | مفتوح — TEST_ACCOUNTS_REQUIRED | المالك يوفّر 5 حسابات (انظر التقرير الرئيسي) |

## P1 — مهم قبل التوسيع

| # | العائق | الشدة | المصدر | الحالة | المسار |
|---|---|---|---|---|---|
| P1-1 | `exam_templates` غير مقيدة بالصف/المنهج (RLS تقرأ أي نموذج نشط؛ `start_exam_session` يتحقق `is_active` فقط) | MEDIUM | WAVE-2 | مفتوح — NEEDS_OWNER_APPROVAL | migration: تحقق grade/track في RLS + RPC |
| P1-2 | `user_progress` لا يُكتب من العميل إطلاقاً ⇒ «واصل المذاكرة» وملخص التقدم يبقيان صفراً؛ شريط «التقدم في الدرس» يقيس توفر المحتوى لا تقدم الطالب (تسمية مضللة) | MEDIUM | WAVE-1 | مفتوح | PR مستقل: استدعاء `grade_lesson_quiz`/تصحيح التسمية — جزء منه UI فقط |
| P1-3 | ~~رأس نتيجة التدريب/السجل يقرأ أعمدة غير موجودة (نسبة خاطئة + عدّادات صفر)~~ | HIGH | WAVE-1 | **مُصلح — PR #27** (بانتظار دمج المالك) | — |
| P1-4 | ~~الجلسة `expired` في الاختبار الصارم تعلق على «جارٍ التسليم التلقائي» إلى الأبد~~ | MEDIUM | WAVE-1 | **مُصلح — PR #28** (بانتظار دمج المالك) | — |
| P1-5 | ~~`/exams` غير موجودة صراحة في denylist الـ SW~~ | LOW→مانع سياسة | WAVE-5 | **مُصلح — PR #29** (بانتظار دمج المالك) | — |
| P1-6 | PR #26 (تقارير الإدارة والإشعارات) ما زال Draft غير مدموج ⇒ WAVE-4 متخطّاة | قرار منتج | WAVE-4 | مفتوح | المالك يبت: دمج أو تأجيل صريح |

## P2 — لاحق

| # | العائق | الشدة | المصدر |
|---|---|---|---|
| P2-1 | مسارا `/grades` و`/grades/$gradeId/subjects` يتمان — لا روابط إليهما من التنقل | LOW | WAVE-1 |
| P2-2 | `StudentProfileCard` كود ميت يحوي CTAs «اشترك الآن/تجديد» (غير قابلة للوصول حالياً) — يُحذف أو يُكيَّف مع المجانية | LOW | WAVE-1 |
| P2-3 | حماية مسارات admin على مستوى المكوّن (`useEffect` redirect) لا على مستوى layout `beforeLoad` — إطار عرض واحد قبل التحويل، بلا كشف بيانات | LOW | WAVE-2 |
| P2-4 | content_manager غير محجوب من مسارات الطالب على مستوى المسار (يرى هيكلاً فارغاً فقط؛ الحماية بياناتية) | LOW | WAVE-2 |
| P2-5 | `units` مقروءة لـ anon (6 صفوف) بينما `subjects` محجوبة — عدم اتساق RLS بسيط يُراجع | LOW | WAVE-3 |
| P2-6 | Preview المحلي يتطلب تثبيت `wrangler` (غير مثبت) — فحص الأجهزة الفعلية مؤجل لما بعد أول Preview | LOW | WAVE-5 |
| P2-7 | عناصر تجميلية: تسمية زر فتح PDF في صفحة الدرس، مكوّن `Section` غير مستخدم | LOW | WAVE-1 |

## قاعدة التنفيذ

- أي P0 يتحول إلى PR مستقل أو NEEDS_OWNER_DECISION — **تحقق**: P0-1 مسجل NEEDS_OWNER_APPROVAL (لا كود)، P0-2/P0-3 إجراءات مالك.
- لا تطوير جديد قبل إغلاق P0 — المرحلة متوقفة عند هذا الحد.
